@model IEnumerable<Youtube.Models.comment>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.comment_content)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.comment_created)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.user.user_username)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.video.video_name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.comment_content)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.comment_created)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.user.user_username)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.video.video_name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.comment_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.comment_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.comment_id })
        </td>
    </tr>
}

</table>
